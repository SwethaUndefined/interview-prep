//A polyfill is a piece of code (usually JavaScript) used to provide modern functionality on older browsers 
// that do not natively support it. It allows developers to use modern 
//features without worrying about compatibility issues in older environments.